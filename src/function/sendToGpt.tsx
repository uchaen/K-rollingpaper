function sendToGpt(
  nickname: string,
  relation: string,
  topic: string,
  include: string,
  polite: boolean
) {
  console.log(nickname, relation, topic, include, polite);
}

export default sendToGpt;
